build*
.vscode
.vs
CMakeSettings.json
rs_driverConfigVersion.cmake
rs_driverConfig.cmake
# Removing this in the Orca fork, so we can just commit this file. In the
# original repo this is always regenerated by a Cmake rule
#version.h
